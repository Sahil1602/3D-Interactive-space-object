# 3D Interactive Space Object

An interactive 3D web application built with Three.js and TypeScript that renders a rotating torus object in space with 200 procedurally generated stars, dynamic lighting, and orbit controls.

Run `npm install` to install dependencies, then `npm run dev` to start the development server. Use mouse to rotate, pan, and zoom the camera around the scene.